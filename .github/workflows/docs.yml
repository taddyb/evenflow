name: docs
on:
  push:
    branches: [main]
    paths:
      - crates/ddrs/docs/book/**
      - crates/ddrs/book.toml
      - .github/workflows/docs.yml
  pull_request:
    paths:
      - crates/ddrs/docs/book/**
      - crates/ddrs/book.toml
      - .github/workflows/docs.yml
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: docs-mdbook
      - name: Install mdbook + preprocessors
        # Pinned: mdbook 0.5.x ships a new preprocessor protocol that breaks
        # both mdbook-katex (0.9.x) and mdbook-mermaid (0.16.x). Hold the
        # whole stack at the last 0.4-compatible versions until the
        # preprocessor ecosystem catches up.
        run: |
          cargo install --locked --version 0.4.52 mdbook
          cargo install --locked --version 0.9.4 mdbook-katex
          cargo install --locked --version 0.16.0 mdbook-mermaid
      - name: Build
        working-directory: crates/ddrs
        run: mdbook build
      - uses: actions/upload-pages-artifact@v3
        if: github.ref == 'refs/heads/main'
        with:
          path: crates/ddrs/target/book

  deploy:
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
