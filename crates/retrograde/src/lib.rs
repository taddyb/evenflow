//! `retrograde` reserves the name of the evenflow operator: the crate that will
//! hold typed model-state resources (spec + status), hash identity over a
//! spec and its dependencies' outputs, stale detection, and a `reconcile`
//! verb that launches the member crates as workers. It is intentionally
//! empty until it gets its own design.
